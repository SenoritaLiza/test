name: food
description: "A new Flutter project."

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.5.3

dependencies:
  flutter:
    sdk: flutter
  provider: ^6.0.5

  flutter_svg: ^1.0.3  # Убедитесь, что это правильно подключено

  cupertino_icons: ^1.0.8

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^4.0.0

flutter:

  uses-material-design: true
  
  assets:
    - picture/
    - icon/

  # fonts:
  #   - family: GothamPro
  #     fonts:
  #       - asset: font/GothamPro.ttf
